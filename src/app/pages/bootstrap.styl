.carousel
    .carousel-item > img
        // object-fit contain
        max-width 100%
        max-height 100%

    .carousel-control-prev, .carousel-control-next
        & > img
            // width 48px
            // height 48px

        &:hover > img
            filter drop-shadow(0 0 4px #fff)
